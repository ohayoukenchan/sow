import SwiftUI

final class SowColors {
    static let primary = Color(UIColor.accentColor.red)
    static let monotone1 = Color(.sow.monotone1)
    static let monotone2 = Color(.sow.monotone2)
    static let monotone3 = Color(.sow.monotone3)
    static let monotone4 = Color(.sow.monotone4)
    static let monotone5 = Color(.sow.monotone5)
    static let monotone6 = Color(.sow.monotone6)
    static let monotone7 = Color(.sow.monotone7)
    static let monotone8 = Color(.sow.monotone8)
}
